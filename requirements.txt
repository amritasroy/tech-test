gitpython>=3.1.40
click>=8.1.7
tabulate>=0.9.0
python-dateutil>=2.8.2

# Optional dependencies for full LLM model support
# The tool works with heuristic-based analysis without these
# Uncomment to enable full transformer model support:
# transformers>=4.30.0
# torch>=2.0.0
